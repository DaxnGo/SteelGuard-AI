# SteelGuard AI frontend prediction source
# Keep true for the current competition frontend MVP.
STEELGUARD_USE_MOCK_API=true

# Optional mock-development settings
# STEELGUARD_MOCK_RESPONSE_PATH=mock/prediction_response.json
# STEELGUARD_MOCK_DELAY_SECONDS=0.6

# Required only after STEELGUARD_USE_MOCK_API is deliberately changed to false.
# The shared timeout values remain TBD and must be confirmed with the backend team.
# STEELGUARD_API_BASE_URL=http://backend:8000
# STEELGUARD_API_CONNECT_TIMEOUT_SECONDS=<confirmed-positive-seconds>
# STEELGUARD_API_READ_TIMEOUT_SECONDS=<confirmed-positive-seconds>
